import * as lucide from 'lucide-react';
console.log(Object.keys(lucide).filter(k => k.toLowerCase().includes('function') || k.toLowerCase().includes('math') || k.toLowerCase().includes('calc') || k.toLowerCase().includes('pi')));
